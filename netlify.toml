[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Next.js redirects for proper routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
